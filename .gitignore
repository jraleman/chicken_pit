# The engine writes its cache into the host project, never in here, but a
# stray import cache should not follow a clone back into godot-base.
.godot/
.import/
*.translation

# System / editor noise
.DS_Store
*~
